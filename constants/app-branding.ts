/** User-facing app name shown in UI, notifications, and store listings. */
export const APP_NAME = "Peak Performance Code";

/** Brand name for the daily motivational quote feature (home card + quote screen). */
export const DAILY_QUOTE_BRAND = "Daily Diesel";

/** Author label for quotes written by the app (not external attributions). */
export const QUOTE_AUTHOR = DAILY_QUOTE_BRAND;
